package
{
   import flash.display.BitmapData;
   
   public dynamic class texture_oldtile extends BitmapData
   {
      public function texture_oldtile()
      {
         super(0,0);
      }
   }
}

import flash.display.BitmapData;

