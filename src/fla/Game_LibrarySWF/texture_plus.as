package
{
   import flash.display.BitmapData;
   
   public dynamic class texture_plus extends BitmapData
   {
      public function texture_plus()
      {
         super(0,0);
      }
   }
}

import flash.display.BitmapData;

