package
{
   import flash.display.BitmapData;
   
   public dynamic class texture_bubble extends BitmapData
   {
      public function texture_bubble()
      {
         super(0,0);
      }
   }
}

import flash.display.BitmapData;

