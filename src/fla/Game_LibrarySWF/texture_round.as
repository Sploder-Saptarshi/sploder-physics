package
{
   import flash.display.BitmapData;
   
   public dynamic class texture_round extends BitmapData
   {
      public function texture_round()
      {
         super(0,0);
      }
   }
}

import flash.display.BitmapData;

