package
{
   import flash.display.BitmapData;
   
   public dynamic class texture_glow extends BitmapData
   {
      public function texture_glow()
      {
         super(0,0);
      }
   }
}

import flash.display.BitmapData;

