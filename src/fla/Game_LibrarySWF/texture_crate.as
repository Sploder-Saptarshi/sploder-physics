package
{
   import flash.display.BitmapData;
   
   public dynamic class texture_crate extends BitmapData
   {
      public function texture_crate()
      {
         super(0,0);
      }
   }
}

import flash.display.BitmapData;

