package
{
   import flash.display.BitmapData;
   
   public dynamic class texture_grain extends BitmapData
   {
      public function texture_grain()
      {
         super(0,0);
      }
   }
}

import flash.display.BitmapData;

