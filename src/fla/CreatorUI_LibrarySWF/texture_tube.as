package
{
   import flash.display.BitmapData;
   
   public dynamic class texture_tube extends BitmapData
   {
      public function texture_tube()
      {
         super(0,0);
      }
   }
}

import flash.display.BitmapData;

