package
{
   import flash.display.BitmapData;
   
   public dynamic class texture_parchment extends BitmapData
   {
      public function texture_parchment()
      {
         super(0,0);
      }
   }
}

import flash.display.BitmapData;

