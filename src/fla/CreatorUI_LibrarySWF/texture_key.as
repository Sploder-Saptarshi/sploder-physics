package
{
   import flash.display.BitmapData;
   
   public dynamic class texture_key extends BitmapData
   {
      public function texture_key()
      {
         super(0,0);
      }
   }
}

import flash.display.BitmapData;

