package
{
   import flash.display.BitmapData;
   
   public dynamic class texture_spikes extends BitmapData
   {
      public function texture_spikes()
      {
         super(0,0);
      }
   }
}

import flash.display.BitmapData;

