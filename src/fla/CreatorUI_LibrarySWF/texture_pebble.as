package
{
   import flash.display.BitmapData;
   
   public dynamic class texture_pebble extends BitmapData
   {
      public function texture_pebble()
      {
         super(0,0);
      }
   }
}

import flash.display.BitmapData;

