package
{
   import flash.display.BitmapData;
   
   public dynamic class texture_gear extends BitmapData
   {
      public function texture_gear()
      {
         super(0,0);
      }
   }
}

import flash.display.BitmapData;

