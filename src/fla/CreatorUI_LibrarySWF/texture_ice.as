package
{
   import flash.display.BitmapData;
   
   public dynamic class texture_ice extends BitmapData
   {
      public function texture_ice()
      {
         super(0,0);
      }
   }
}

import flash.display.BitmapData;

