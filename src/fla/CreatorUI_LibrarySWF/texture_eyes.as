package
{
   import flash.display.BitmapData;
   
   public dynamic class texture_eyes extends BitmapData
   {
      public function texture_eyes()
      {
         super(0,0);
      }
   }
}

import flash.display.BitmapData;

