package
{
   import flash.display.BitmapData;
   
   public dynamic class texture_marble extends BitmapData
   {
      public function texture_marble()
      {
         super(0,0);
      }
   }
}

import flash.display.BitmapData;

