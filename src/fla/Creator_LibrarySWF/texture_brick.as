package
{
   import flash.display.BitmapData;
   
   public dynamic class texture_brick extends BitmapData
   {
      public function texture_brick()
      {
         super(0,0);
      }
   }
}

import flash.display.BitmapData;

