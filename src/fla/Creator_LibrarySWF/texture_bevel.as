package
{
   import flash.display.BitmapData;
   
   public dynamic class texture_bevel extends BitmapData
   {
      public function texture_bevel()
      {
         super(0,0);
      }
   }
}

import flash.display.BitmapData;

