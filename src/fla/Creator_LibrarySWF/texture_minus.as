package
{
   import flash.display.BitmapData;
   
   public dynamic class texture_minus extends BitmapData
   {
      public function texture_minus()
      {
         super(0,0);
      }
   }
}

import flash.display.BitmapData;

