package
{
   import flash.display.BitmapData;
   
   public dynamic class texture_boulder extends BitmapData
   {
      public function texture_boulder()
      {
         super(0,0);
      }
   }
}

import flash.display.BitmapData;

