package
{
   import flash.display.BitmapData;
   
   public dynamic class texture_eye extends BitmapData
   {
      public function texture_eye()
      {
         super(0,0);
      }
   }
}

import flash.display.BitmapData;

