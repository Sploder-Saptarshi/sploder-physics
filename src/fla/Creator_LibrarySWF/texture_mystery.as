package
{
   import flash.display.BitmapData;
   
   public dynamic class texture_mystery extends BitmapData
   {
      public function texture_mystery()
      {
         super(0,0);
      }
   }
}

import flash.display.BitmapData;

