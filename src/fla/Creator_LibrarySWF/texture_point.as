package
{
   import flash.display.BitmapData;
   
   public dynamic class texture_point extends BitmapData
   {
      public function texture_point()
      {
         super(0,0);
      }
   }
}

import flash.display.BitmapData;

