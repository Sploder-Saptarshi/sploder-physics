package
{
   import flash.display.BitmapData;
   
   public dynamic class texture_paper extends BitmapData
   {
      public function texture_paper()
      {
         super(0,0);
      }
   }
}

import flash.display.BitmapData;

