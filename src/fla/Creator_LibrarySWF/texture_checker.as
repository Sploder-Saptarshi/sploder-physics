package
{
   import flash.display.BitmapData;
   
   public dynamic class texture_checker extends BitmapData
   {
      public function texture_checker()
      {
         super(0,0);
      }
   }
}

import flash.display.BitmapData;

