package
{
   import flash.display.BitmapData;
   
   public dynamic class texture_bomb extends BitmapData
   {
      public function texture_bomb()
      {
         super(0,0);
      }
   }
}

import flash.display.BitmapData;

