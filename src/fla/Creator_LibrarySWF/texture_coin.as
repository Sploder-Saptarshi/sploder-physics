package
{
   import flash.display.BitmapData;
   
   public dynamic class texture_coin extends BitmapData
   {
      public function texture_coin()
      {
         super(0,0);
      }
   }
}

import flash.display.BitmapData;

